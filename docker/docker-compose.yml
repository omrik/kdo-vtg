services:
  kdo-vtg:
    image: ghcr.io/omrik/kdo-vtg:latest
    container_name: kdo-vtg
    ports:
      - "8080:8000"
    volumes:
      - kdo-vtg_config:/app/config
      - /volume1/media:/media:ro
    environment:
      - TZ=UTC
      - PUID=1000
      - PGID=100
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    restart: unless-stopped

volumes:
  kdo-vtg_config:
